{
  "kind": "collectionType",
  "collectionName": "portal_accounts",
  "info": {
    "singularName": "portal-account",
    "pluralName": "portal-accounts",
    "displayName": "Portal Account",
    "description": "Client portal logins, created by the desk once a trading account exists. The desk attaches the MT5 number, so ownership is established by construction and no approval queue is needed. NEVER expose to the Public role \u2014 holds a password hash. `inviteUrl` is the link to send the client if the email fails."
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "name": {
      "type": "string"
    },
    "email": {
      "type": "string",
      "required": true,
      "unique": true
    },
    "passwordHash": {
      "type": "string",
      "required": false
    },
    "inviteToken": {
      "type": "string"
    },
    "inviteExpiresAt": {
      "type": "datetime"
    },
    "inviteUrl": {
      "type": "string"
    },
    "inviteSentAt": {
      "type": "datetime"
    },
    "inviteEmailError": {
      "type": "string"
    },
    "mt5Login": {
      "type": "biginteger",
      "unique": true
    },
    "mt5Name": {
      "type": "string"
    },
    "linkStatus": {
      "type": "enumeration",
      "enum": [
        "pending",
        "approved",
        "rejected"
      ],
      "default": "pending",
      "required": true
    },
    "reviewedAt": {
      "type": "datetime"
    },
    "reviewedBy": {
      "type": "string"
    },
    "reviewNote": {
      "type": "string"
    },
    "referralCode": {
      "type": "string"
    },
    "referralStatus": {
      "type": "enumeration",
      "enum": [
        "pending",
        "approved",
        "rejected"
      ],
      "default": "pending",
      "required": true
    },
    "referralReviewNote": {
      "type": "string"
    },
    "lastLoginAt": {
      "type": "datetime"
    },
    "cashbackClaims": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::cashback-claim.cashback-claim",
      "mappedBy": "portalAccount"
    },
    "referralClaims": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::referral-claim.referral-claim",
      "mappedBy": "portalAccount"
    }
  }
}
